<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Task</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/mystyles.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap" rel="stylesheet">
  <link rel="icon" href="./imgs/flavicon.png" type="image/x-icon">
</head>
<body>
  <%- include('./partials/_navbar') %>

  <% if (Task.length > 0) { %>
    <% const task = Task[0]; %>
    <% const formattedDate = dayjs(task.Task_due_date).format('YYYY-MM-DD'); %>

    <div class="container mt-5 pt-5">
      <div class="card view-task-card">
        <div class="card-body">
          <h2 class="card-title text-center"><%= task.Task_name %></h2>
          <p class="card-text text-center"><strong>Description:</strong> <%= task.Task_description %></p>
          <p class="card-text text-center"><strong>Priority:</strong> <%= task.Priority_name %></p>
          <p class="card-text text-center"><strong>Due Date:</strong> <%= formattedDate %></p>
          <div class="d-flex justify-content-center">
            <a href="/" class="btn btn-primary">Back to Tasks</a>
          </div>
        </div>
      </div>
    </div>
  <% } else { %>
    <div class="container mt-5 pt-5">
      <div class="alert alert-warning text-center" role="alert">
        No task found.
      </div>
      <div class="d-flex justify-content-center">
        <a href="/" class="btn btn-primary">Back to Tasks</a>
      </div>
    </div>
  <% } %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>

  <%- include('./partials/_footer') %>
</body>
</html>