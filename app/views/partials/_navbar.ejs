<nav class="navbar navbar-expand-lg py-3 fixed-top">
    <div class="container-fluid">
        <h1 class="navbar-brand" href="#" style="color: white">Task Manager</h1>
        <button class="navbar-toggler" style="color: white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="true" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse w-100" id="navbarNavAltMarkup">
            <div class="navbar-nav w-100"> 
                <% if (loggedin) { %>
                    <a class="nav-link" href="/"><%= username %></a>
                    <a class="nav-link" href="/new">Add Task</a>
                    <a class="nav-link" href="/stats">Stats</a>
                    <a class="nav-link" href="/settings">Settings</a>
                    <a class="nav-link" href="/logout">Logout</a>
                <% } else { %>
                    <a class="nav-link" href="/login">Login</a>
                <% } %>
            </div>
            <div>
                <button type="button" class="btn btn-secondary" id="infoPopover" data-bs-toggle="popover" data-bs-placement="left" data-bs-content="This app helps you manage your tasks efficiently. You can add, edit, and delete tasks, as well as view statistics on your task completion.">
                    Info
                </button>
            </div>
        </div>
    </div>
</nav>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        // Initialize popover
        var popoverTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="popover"]'))
        var popoverList = popoverTriggerList.map(function (popoverTriggerEl) {
            return new bootstrap.Popover(popoverTriggerEl)
        })
    });
</script>
